extends layout

block content
  head
    title Crear Usuario
  body
    .container
      h1 Crear Usuario

      form#crear-usuario-form(action="/admin/crear-usuario" method="POST")
        .form-group
          label(for="nombre") Nombre:
          input#nombre.form-control(type="text" name="nombre")

        .form-group
          label(for="correo_electronico") Correo Electrónico:
          input#correo_electronico.form-control(type="email" name="correo_electronico")

        .form-group
          label(for="password") Contraseña:
          input#password.form-control(type="password" name="password")

        .form-group
          label(for="rol") Rol:
          select#rol.form-control(name="rol")
            option(value="recepcionista") Recepcionista
            option(value="tecnico") Técnico
            option(value="bioquimico") Bioquímico
            option(value="admin") Administrador
            option(value="paciente") Paciente

        button.btn.btn-primary(type="submit" onclick="return confirm('¿Estás seguro de crear el usuario?')") Crear Usuario

      if mensaje
        p(style="color: green; margin-top: 20px;") #{mensaje}
      if error
        p(style="color: red; margin-top: 20px;") #{error}
